name: React CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

# Automatically cancel out-of-date builds on the same PR/branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install --no-audit --no-fund

      # Optional: Catch syntax & code style issues early
      - name: Run Linter
        run: npm run lint --if-present

      # Optional: Run test suite if you have tests configured
      - name: Run Tests
        run: npm test --if-present -- --watchAll=false

      - name: Build Project
        run: npm run build